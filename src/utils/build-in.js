/*export default {
  setTimeout: window.setTimeout,
  setInterval: window.setInterval,
  requestAnimationFrame: window.requestAnimationFrame,
  fetch: window.fetch,
  xhrPrototypeOpen: XMLHttpRequest.prototype.open,
  xhrPrototypeSend: XMLHttpRequest.prototype.send,
  addEventListener: window.addEventListener,
  removeEventListener: window.removeEventListener
}*/

export const setTimeout = window.setTimeout
export const setInterval = window.setInterval
export const requestAnimationFrame = window.requestAnimationFrame
export const fetch = window.fetch
export const xhrPrototypeOpen = XMLHttpRequest.prototype.open
export const xhrPrototypeSend = XMLHttpRequest.prototype.send
export const addEventListener = window.addEventListener
export const removeEventListener = window.removeEventListener
